---
- hosts: linux_servers
  become: true
  tasks:
    - name: "Shell command Crafty"
      shell: 
        cmd: "cd /tmp && git clone https://gitlab.com/crafty-controller/crafty-installer-4.0.git && cd crafty-installer-4.0 && sudo ./install_crafty.sh"
